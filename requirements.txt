# ─────────────────────────────────────────────────────────────────
# MARL Exploración 3D — Dependencias completas
# ─────────────────────────────────────────────────────────────────
# Instalación rápida:
#   pip install -r requirements.txt
#
# Con GPU (CUDA 12.x):
#   pip install -r requirements.txt
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# ─────────────────────────────────────────────────────────────────

# ── Núcleo científico ─────────────────────────────────────────────
numpy>=1.24,<3.0
scipy>=1.10

# ── Entorno de RL ─────────────────────────────────────────────────
gymnasium>=0.29,<2.0

# ── Algoritmo PPO (Stable-Baselines3) ────────────────────────────
# "extra" incluye: tensorboard, rich, pandas, matplotlib (opcionalmente)
stable-baselines3[extra]>=2.3,<3.0

# ── Deep learning (backend de SB3) ───────────────────────────────
# CPU por defecto; sustituir por la variante CUDA si hay GPU disponible
torch>=2.1,<3.0

# ── Generación de vídeo ───────────────────────────────────────────
opencv-python>=4.8     # cv2: renderizado de frames y escritura MP4

# ── Visualización / métricas (opcionales pero usados en train_all) ─
matplotlib>=3.7
tensorboard>=2.14      # monitorización de entrenamiento SB3